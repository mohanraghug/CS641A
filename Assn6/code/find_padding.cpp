#include <bits/stdc++.h>
using namespace std;

int main()
{

    int x;
    int count = 0;
    while (cin >> hex >> x)
    {
        count++;
        cout << char(x);
    }
    cout << endl;

    return 0;
}

// You see a Gold-Bug in one corner. It is the key to a treasure found by